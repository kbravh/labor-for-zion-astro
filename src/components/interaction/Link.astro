---
interface Props {
	href: string;
	target?: "_blank" | "_self" | "_parent" | "_top";
	rel?: string;
	className?: string;
}

const { href, target, rel, className } = Astro.props;
---

<a
  {href}
  {target}
  {rel}
  class:list={[
    "inline-flex items-center gap-2",
    "focus:outline-none focus-visible:outline-none focus-visible:ring-emerald-500 focus-visible:ring-2",
    "active:underline active:font-bold decoration-emerald-500 dark:decoration-emerald-300 decoration-0 active:decoration-2",
    className,
  ]}
>
  <slot />
</a>
